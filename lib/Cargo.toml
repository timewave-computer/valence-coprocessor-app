[package]
name = "valence-coprocessor-app-lib"
version = "0.1.0"
edition = "2024"

[dependencies]
valence-coprocessor-app = { path = "../core", features = ["lib"] }
valence-coprocessor-wasm = { workspace = true, features = ["abi-handlers"] }

[lib]
crate-type = ["cdylib"]
